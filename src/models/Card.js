class Card {
    constructor ( number, month, year, cod, propietary, idProp ) {
        this.number = number,
        this.month = month,
        this.year = year,
        this.cod = cod,
        this.propietary = propietary,
        this.idProp = idProp
    }
}

export default Card;