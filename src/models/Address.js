class Address {
    constructor(id, title, lat, lng, address) {
        this.id = id.toString()
        this.title = title
        this.lat = lat
        this.lng = lng
        this.address = address
    }
}

export default Address;